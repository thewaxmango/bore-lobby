[package]
name = "socket-lobby-server"
version = "0.1.0"
edition = "2021"

[dependencies]
socket-lobby-common = { path = "../common" }
socket-lobby-proset = { path = "../games/proset" }
serde_json = { workspace = true }
tokio = { workspace = true }
rand = { workspace = true }
bore-cli = "0.6"
